require 'spec_helper'

describe ActivePresent do
  it 'has a version number' do
    expect(ActivePresent::VERSION).not_to be nil
  end

  # it 'does something useful' do
  #   expect(false).to eq(true)
  # end
end
